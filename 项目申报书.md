# moonbit_gm 项目申报书

> 依据《2026 MoonBit 国产基础软件开源大赛章程》及 osc2026-guide skill 申报模板撰写。
> 申报语言：中文（章程要求），正文保持简洁、聚焦“做什么 / 为什么 / 怎么做 / 交付什么”。

## 基本信息

- 项目名称：moonbit_gm：国密 SM2/SM3/SM4 算法库的 MoonBit 移植
- 参赛者：rzhangsan（Pai2s）
- GitHub 仓库链接：https://github.com/nrzhangsan/moonbit_gm.git
- Gitlink 仓库链接：https://gitlink.org.cn/nrzhangsan/moonbit_gm.git
- 项目方向：MoonBit 密码学基础库 / 国密算法基础设施
- 是否为移植项目：是

## 项目简介

moonbit_gm 计划将 Go 语言生态中成熟的国密算法库 `tjfoc/gmsm`（Apache-2.0）的核心能力移植到 MoonBit 生态，为需要在 MoonBit 中使用中国国家密码管理局发布的商用密码算法（SM2 椭圆曲线公钥算法、SM3 密码杂凑算法、SM4 分组密码算法）的库作者与应用开发者，提供纯 MoonBit 实现、可测试、可复用的国密基础库。

项目面向国产密码合规场景，目标是让 MoonBit 开发者无需依赖 Go/C 运行时即可在 WebAssembly、Native 等多后端上完成国密签名、杂凑与对称加密。相比“把 Go 库编译成 WASM 再调用”的包装方案，本项目提供语义清晰、可被 `moon test` 覆盖、可被 mooncakes.io 分发的原生 MoonBit API。

## 项目方向与适用场景

- **方向**：MoonBit 密码学基础库，属于“基础数据结构与算法 / 系统能力”类可复用生态组件。
- **适用场景**：
  - 国密合规需求的 Web、服务端、嵌入式应用中的签名与验签；
  - 数据完整性校验与数字证书（X.509）相关的杂凑计算；
  - 需要对敏感数据进行对称加密 / 解密的客户端或边缘计算场景；
  - 作为其它 MoonBit 生态项目（TLS、PKI、区块链、签章）的底层密码学依赖。

## 拟实现的核心功能

- **SM3 密码杂凑算法**
  - 实现标准 SM3 压缩函数与消息填充；
  - 提供 `sm3_sum(bytes) -> Bytes` 与 `Hash` 风格的流式接口。
- **SM4 分组密码算法**
  - 实现 SM4 轮函数、密钥扩展与 ECB/CBC 模式；
  - 提供 PKCS7 填充工具与 `encrypt/decrypt` 基础接口；
  - 预留 GCM 模式（参考上游 `sm4_gcm`）作为后续扩展。
- **SM2 椭圆曲线公钥算法**
  - 实现基于推荐参数（p, a, b, n, Gx, Gy）的椭圆曲线点运算；
  - 支持密钥生成、`sign/verify` 数字签名（Z 值预处理）；
  - 支持基于 SM3 的摘要签名与公钥/私钥基本编解码。
- **测试与示例**
  - 使用官方标准测试向量（GB/T 32918、GB/T 32905）编写回归测试；
  - 提供不少于 1 个可直接 `moon run` 的最小使用示例（如“对一段消息签名并验签”）。
- **工程与合规**
  - 提供 README（目标 / 安装 / 用法 / 示例）；
  - 接入 GitHub Actions 风格 CI 覆盖 `moon check` / `moon test`；
  - 根目录提供 LICENSE（Apache-2.0），并在 README 注明上游来源与参考范围；
  - 准备发布到 mooncakes.io。

## 是否为原创 / 移植 / 参考项目

- **类型**：移植项目（port）。
- 参考项目名称：tjfoc/gmsm
- 原项目链接：https://github.com/tjfoc/gmsm
- 原项目许可证：Apache License 2.0
- 本项目许可证：Apache License 2.0（与原项目一致，符合许可证要求）

与原项目相比，本项目会做以下简化与重新设计：

- 使用 MoonBit 原生包结构、代数数据类型与 `moon test` 测试方式组织代码，而非复刻 Go 的包/接口工程结构；
- 优先实现可在 MoonBit 多后端（WASM / Native）独立运行的核心算法，弱化上游对 Go `crypto` 标准库、x509 证书链、gmtls 传输层等依赖；
- 对上游 `gmtls`、`x509` 证书体系、`pkcs12` 等当前暂不在首期范围的模块保留后续扩展说明，首期聚焦 SM2/SM3/SM4 三大算法本身；
- 对 Go 的 `big.Int`、接口、`error`、切片语义进行 MoonBit 化改写，保证无外部闭源/私有代码依赖。

## 提交记录与仓库要求

- GitHub 与 Gitlink 仓库保持同步，默认分支为 `main`；
- 计划保持 10–20 个连续、有意义的有效 commits（算法分步实现 + 测试 + 文档），不通过空提交、重复提交或无意义拆分满足次数要求；
- 仓库包含完整源代码、README、LICENSE 与 CI 配置。

---

### 附：申报要点对照（自查清单）

| 章程申报项 | 对应内容 |
| --- | --- |
| 项目名称 | moonbit_gm：国密 SM2/SM3/SM4 算法库的 MoonBit 移植 |
| 项目简介 | 见“项目简介”节 |
| 项目方向与适用场景 | 见“项目方向与适用场景”节 |
| 拟实现核心功能 | 见“拟实现的核心功能”节 |
| 原创/移植/参考 | 移植项目（基于 tjfoc/gmsm） |
| 上游名称/链接/许可证 | tjfoc/gmsm · Apache-2.0 · 见“是否为原创…”节 |
| GitHub 仓库链接 | https://github.com/nrzhangsan/moonbit_gm.git |
| Gitlink 仓库链接 | https://gitlink.org.cn/nrzhangsan/moonbit_gm.git |
